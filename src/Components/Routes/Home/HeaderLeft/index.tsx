import React from "react";
import { Text } from "react-native";

const Header = () => {
  return (
    <Text
      style={{
        fontWeight: "bold",
        fontSize: 28,
        paddingLeft: 10,
      }}
    >
      Instagram
    </Text>
  );
};

export default Header;
